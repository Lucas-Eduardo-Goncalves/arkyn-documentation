import { styled } from "@linaria/react";

const RootContentContainer = styled.div`
  width: 100%;
  height: max-content;
  min-height: calc(100vh - 60px);

  padding-top: 60px;
`;

export { RootContentContainer };
