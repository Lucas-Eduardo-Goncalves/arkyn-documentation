# ToastProvider

import { AlertContainer } from "@arkyn/components/alertContainer";
import { AlertContent } from "@arkyn/components/alertContent";
import { AlertDescription } from "@arkyn/components/alertDescription";
import { AlertIcon } from "@arkyn/components/alertIcon";
import { AlertTitle } from "@arkyn/components/alertTitle";
import { ToastProvider } from "@arkyn/components/toastProvider";
import { useToast } from "@arkyn/components/useToast";

import { TextExample } from "../../_examples/text.example";

ToastProvider exposes toast notification actions and renders the global toaster container.

```tsx
import { ToastProvider } from "@arkyn/components/toastProvider";
```

Learn how subpath and root imports differ in [How do I use imports](/docs/guides/how-do-i-use-imports).

## Basic Usage

<div className="noContent">
  <ToastProvider>
    <TextExample content="Application with toast support" />
  </ToastProvider>
</div>

```tsx
<ToastProvider>
    <App />
</ToastProvider>
```

## Triggering Toast Notifications

Use `useToast()` inside provider scope to show success and danger notifications.

<div className="noContent">
  <ToastProvider>
    <TextExample content="Toast actions available via context" />
  </ToastProvider>
</div>

```tsx
function SaveAction() {
    const { showToast } = useToast();

    return (
        <button
            onClick={() =>
                showToast({
                    message: "Saved successfully",
                    type: "success",
                })
            }
        >
            Save
        </button>
    );
}
```

## Toast Types

The provider supports two schemas with predefined visual styles.

<div className="noContent row">
  <div>success</div>
  <div>danger</div>
</div>

```tsx
showToast({ message: "Operation completed", type: "success" });
showToast({ message: "Something went wrong", type: "danger" });
```

## Notes

<AlertContainer scheme="info">
  <AlertIcon />
  <AlertContent>
    <AlertTitle>Hook dependency</AlertTitle>
    <AlertDescription>
      The <code>useToast</code> hook must be called inside{" "}
      <code>ToastProvider</code>. Outside provider scope, it throws an error.
    </AlertDescription>
  </AlertContent>
</AlertContainer>

## Properties

**`children`** - `ReactNode`
React subtree that can access toast context and toaster rendering.
